# Chrome Web Store listing - Shopify Store Inspector

Generated by tools/make-listings.mjs. Paste each block into the field it names.

---

## 1. Name

> Shopify Store Inspector - Theme, Apps & Product Data

`52/75 characters.` The phrase after the dash is what the
store's search matches on. Do not turn it into a keyword list - that gets rejected.

## 2. Short description

> Inspect any Shopify storefront: theme, currency, products and apps. 100% local - no tracking, no data collected.

`112/132 characters.`

## 3. Category

> Developer Tools

## 4. Language

> English (United States)

---

## 5. Detailed description

Plain text, no Markdown - the store renders it literally. Copy from the line
below the rule to the one above the next rule.

---

The theme, the apps, the product data and what is wrong with the page - on any Shopify store.

Theme, currency, product and app details of any Shopify storefront.

WHAT IT DOES

• Shop domain, currency, locale, country and whether a customer is logged in
• Theme name, version, ID, and whether it came from the Theme Store or is bespoke
• A warning when the theme is not the live one, or when you are inside the theme editor preview
• Page type and resource, product ID, vendor, type and variant count
• Variant table with SKUs and prices, correctly converted from Shopify's integer cents
• Around two dozen Shopify apps, detected from the scripts they load

Copy puts a Markdown report on your clipboard. CSV downloads every field.

PRIVACY

This extension collects nothing. No account, no sign-up, no analytics, no
telemetry, no servers. It reads the page you explicitly ask it to inspect,
keeps the result in the popup's memory, and forgets it when you close the
popup. Nothing is written to disk unless you click an export button, and
nothing is ever transmitted anywhere.

PERMISSIONS

• activeTab - gives access to the current tab only at the moment you click the
  icon, and only until you navigate away. This is why the extension needs no
  access to your browsing history and no permission to run on every site.
• scripting - required to read the page once you have clicked the icon.

That is the complete list - two permissions. There is no storage, no
background process and no host permissions.

FREE AND OPEN SOURCE

No account, no sign-up, no trial, no paid tier, no upsell. Every feature
works the moment you install it.

The source is open, MIT licensed: github.com/swanca/chrome-inspector-suite

PAGES IT CANNOT READ

Chrome forbids extensions on its own pages - chrome://, the Web Store, and
view-source. The extension says which case you have hit rather than failing
silently. Local files work if you enable "Allow access to file URLs" on its
details page.

---

## 6. Screenshots

At least one is required. 1280x800 or 640x400, PNG or JPEG. These are the
single biggest factor in whether someone installs, so shoot all five.

1. The Audit tab on a real product page, showing findings with their FAIL and WARN badges.
2. The Details tab: shop, currency, theme name and whether it came from the Theme Store.
3. The Variants tab: a table of SKUs, prices and stock.
4. The Apps detected section, with a dozen app chips visible.
5. A non-Shopify page, showing the extension says so plainly rather than guessing.

Shoot at 1280x800: set Chrome to that window size, open the popup, and
capture the popup together with enough of the page behind it to show the
context. A popup alone on white looks like a screenshot of nothing.

---

## 7. Privacy practices tab

The tab that most often sends a submission back. Every answer here is "no".

**Single purpose**

> Reads and reports the theme, product and app data that a Shopify storefront publishes about itself.

**Permission justifications**

| Permission | Paste this |
|---|---|
| `activeTab` | The extension reads the page the user is on, but only after they click its toolbar icon. activeTab grants that access for that one tab and only until navigation, which avoids requesting host permissions for every site. |
| `scripting` | Used to run a single read-only collector function in the page once the user has clicked the icon. It reads the DOM and returns data to the popup. It never modifies the page. |

**Remote code**

> No. All code is included in the package. Nothing is fetched or evaluated at runtime.

**Data usage** - tick nothing. Then declare:

- [x] I do not sell or transfer user data to third parties, outside of the approved use cases
- [x] I do not use or transfer user data for purposes that are unrelated to my item's single purpose
- [x] I do not use or transfer user data to determine creditworthiness or for lending purposes

---

## 8. Searches this listing targets

Not to be stuffed anywhere. They are what the name and the description were
written to answer.

- shopify theme detector
- what shopify theme is this
- shopify apps detector
- shopify spy
- shopify store inspector
- shopify theme finder
- shopify product data

---

## 9. Package to upload

```bash
cd extensions/shopify-store-inspector
zip -r ../../shopify-store-inspector.zip manifest.json icons src
```

Ship only those three. `test/`, `README.md` and this file are development
artefacts and only slow the review down.

---

## 10. Before you press submit

- [ ] Version bumped in `tools/extensions.mjs`, then `node tools/scaffold.mjs`
- [ ] `npm test` and `npm run check` both green
- [ ] Loaded unpacked once and the popup actually opened
- [ ] Five screenshots taken at 1280x800
- [ ] Privacy tab filled in exactly as above
- [ ] A support email you will read set on the account
